name: Cowsay
on:
  # push:
  workflow_dispatch:
    inputs:
      name:
        description: 'Test'
        default: 'x-cmd'

jobs:
  cowsay:
    runs-on: ubuntu-latest
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      # use dev branch x-cmd source
      ___X_CMD_CODESORUCE_REPO: dev
      CONCURRENCY: 10
    steps:
      - uses: x-cmd/action@main
        with:
          code: |
            echo "Hello World"
            ls
            x cowsay "hello world"
            echo "${{ github.event.inputs.name }}"
